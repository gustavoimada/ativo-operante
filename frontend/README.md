# Ativo Operante Frontend

Frontend em Vue 3 para o sistema Ativo Operante.

## Comandos

```powershell
npm install
npm run serve
npm run build
```

## Configuracao

Use `VUE_APP_API_URL` para apontar o frontend para a API:

```powershell
$env:VUE_APP_API_URL="http://localhost:8080"
```
